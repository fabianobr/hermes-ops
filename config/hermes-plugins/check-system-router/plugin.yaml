manifest_version: 1
name: check-system-router
version: "0.1.3"
description: "Expose and route deterministic check-system requests before LLM dispatch."
author: "hermes-ops"
kind: standalone
provides_hooks:
  - pre_gateway_dispatch
